# Banner

## Status: fallback sem ANSI (v0.1.0-alpha)

O spike de renderização ANSI falhou no terminal do Claude Code (escapes apareceram literais como texto). Decisão: usar banner monocromático pra esta versão.

**Revisitar na v0.1.0 final** — se Anthropic publicar suporte nativo a ANSI truecolor no renderer do Claude Code, regerar com `FORCE_COLOR=3`.
